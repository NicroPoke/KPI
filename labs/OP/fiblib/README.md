# kpi-fiblib

Reusable library extracted from Task 1.

## Features

- `fibonacci_generator()` — infinite Fibonacci number generator.
- `consume_with_timeout(iterator, timeout_seconds, iteration_delay_seconds=0.2)` — consumes values from any iterator for a limited time and prints running stats for numeric values.
