# Lab 04 — Bi-Directional Priority Queue

A bi-directional priority queue is implemented.

## Features

- `enqueue(item, priority)`
- `peek(...)`: `highest`, `lowest`, `oldest`, `newest`
- `dequeue(...)`: `highest`, `lowest`, `oldest`, `newest`

The default mode is `highest`.

