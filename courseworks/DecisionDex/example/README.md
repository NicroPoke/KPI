# DecisionDex Example

This folder is a small local-dependency demo for the DecisionDex core package.

## Install

```bash
npm install
```

## Run

```bash
npm run demo
```

The demo loads `decisiondex-core` from `../core` and prints a recommendation, a ranked list, and a small generator sample.